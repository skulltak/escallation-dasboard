services:
  - type: web
    name: escalation-dashboard-3d
    runtime: node
    buildCommand: ./render-build.sh
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 10000
      - key: NODE_VERSION
        value: 22.12.0
